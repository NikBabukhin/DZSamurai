import React from 'react'

function AlternativeMessage() {
    return (
        <div>Не, ну в целом, было не трудно, но я сначала решил передавать в качестве пропсов не отдельные значения, а
            целиком весь объект, это не самое лучшее решение но типизация прошла на ура и все работало</div>
    )
}

export default AlternativeMessage
